<form method="post" action="{{ path('app_setting_unit_price_delete', {'id': setting_unit_price.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ setting_unit_price.id) }}">
    <button class="btn">Delete</button>
</form>
