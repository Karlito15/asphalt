<form method="post" action="{{ path('app_race_season_delete', {'id': race_season.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ race_season.id) }}">
    <button class="btn">Delete</button>
</form>
