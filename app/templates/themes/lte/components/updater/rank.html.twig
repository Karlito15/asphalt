
<!-- component HTML -->
<div class="form-floating"{{ attributes }}>
    {{ form_widget(formulaire) }}
    {{ form_label(formulaire) }}
</div>
{#{{ form_help(formulaire) }}#}
{#{{ form_errors(formulaire) }}#}
