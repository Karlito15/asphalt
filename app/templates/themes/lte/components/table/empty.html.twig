{#<div{{ attributes }}>#}
    <!-- component HTML -->
    <tr>
        <td colspan="{{ value }}" class="{{ classes.table.empty }}">
            {{ 'text.no-data'|trans }}
        </td>
    </tr>
{#</div>#}
