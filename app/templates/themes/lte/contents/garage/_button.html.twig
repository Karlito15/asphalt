
<div class="input-group mt-3">
    <a
        href="{{ path(links.index) }}"
        class="{{ classes.button.back }}"
    >
        {{ 'btn.back'|trans }}
    </a>
    <button
        type="submit"
        class="{{ classes.button.success }}"
    >
        {{ 'btn.register'|trans }}
    </button>
</div>
