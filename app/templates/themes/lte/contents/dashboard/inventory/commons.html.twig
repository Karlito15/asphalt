
    <h4 class="text-start text-uppercase">
        {{ 'text.common'|trans|plural }}
    </h4>

    <div class="row row-cols-1 row-cols-md-4 g-md-3 mb-md-1">
        <twig:FormDashboardImport maxValue="{{ 999 }}" letter="{{ null }}" icon="{{ icons.speed }}" entity="{{ commons.54.viewData }}" />
        <twig:FormDashboardImport maxValue="{{ 999 }}" letter="{{ null }}" icon="{{ icons.acceleration }}" entity="{{ commons.55.viewData }}" />
        <twig:FormDashboardImport maxValue="{{ 999 }}" letter="{{ null }}" icon="{{ icons.handling }}" entity="{{ commons.56.viewData }}" />
        <twig:FormDashboardImport maxValue="{{ 999 }}" letter="{{ null }}" icon="{{ icons.nitro }}" entity="{{ commons.57.viewData }}" />
    </div>
