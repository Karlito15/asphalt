
    <h3 class="text-end text-uppercase bungee-shade-regular">
        {{ 'text.common'|trans|plural }}
    </h3>
    <div class="row row-cols-1 row-cols-md-4 g-md-3 mb-md-1">
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="{{ null }}" icon="{{ icons.rocket }}" entity="{{ commons.54.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="{{ null }}" icon="{{ icons.gauge }}" entity="{{ commons.55.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="{{ null }}" icon="{{ icons.gear }}" entity="{{ commons.56.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="{{ null }}" icon="{{ icons.fire }}" entity="{{ commons.57.viewData }}" />
    </div>
