
    <h3 class="text-end text-uppercase bungee-shade-regular">
        {{ 'text.rare'|trans|plural }}
    </h3>
    <div class="row row-cols-1 row-cols-md-5 g-md-3 mb-md-1">
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-a" icon="{{ icons.rocket }}" entity="{{ rares.34.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-b" icon="{{ icons.rocket }}" entity="{{ rares.35.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-c" icon="{{ icons.rocket }}" entity="{{ rares.36.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-d" icon="{{ icons.rocket }}" entity="{{ rares.37.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-s" icon="{{ icons.rocket }}" entity="{{ rares.38.viewData }}" />
    </div>
    <div class="row row-cols-1 row-cols-md-5 g-md-3 mb-md-1">
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-a" icon="{{ icons.gauge }}" entity="{{ rares.39.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-b" icon="{{ icons.gauge }}" entity="{{ rares.40.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-c" icon="{{ icons.gauge }}" entity="{{ rares.41.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-d" icon="{{ icons.gauge }}" entity="{{ rares.42.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-s" icon="{{ icons.gauge }}" entity="{{ rares.43.viewData }}" />
    </div>
    <div class="row row-cols-1 row-cols-md-5 g-md-3 mb-md-1">
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-a" icon="{{ icons.gear }}" entity="{{ rares.44.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-b" icon="{{ icons.gear }}" entity="{{ rares.45.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-c" icon="{{ icons.gear }}" entity="{{ rares.46.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-d" icon="{{ icons.gear }}" entity="{{ rares.47.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-s" icon="{{ icons.gear }}" entity="{{ rares.48.viewData }}" />
    </div>
    <div class="row row-cols-1 row-cols-md-5 g-md-3 mb-md-1">
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-a" icon="{{ icons.fire }}" entity="{{ rares.49.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-b" icon="{{ icons.fire }}" entity="{{ rares.50.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-c" icon="{{ icons.fire }}" entity="{{ rares.51.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-d" icon="{{ icons.fire }}" entity="{{ rares.52.viewData }}" />
        <twig:UpdaterImport maxValue="{{ 999 }}" letter="fa-s" icon="{{ icons.fire }}" entity="{{ rares.53.viewData }}" />
    </div>
