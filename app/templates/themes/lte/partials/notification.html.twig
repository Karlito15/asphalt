{% for label, messages in app.flashes %}
    {% for message in messages %}
{#        <twig:AlertBootstrap type="{{ label }}" message="{{ message }}" />#}
        <twig:AlertSweet type="{{ label }}" message="{{ message }}" />
    {% endfor %}
{% endfor %}
