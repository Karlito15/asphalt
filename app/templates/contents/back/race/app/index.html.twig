{% extends 'base.html.twig' %}

{% block title %}RaceApp index{% endblock %}

{% block body %}
    <h1>RaceApp index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for race_app in race_apps %}
            <tr>
                <td>{{ race_app.id }}</td>
                <td>
                    <a href="{{ path('app_race_app_show', {'id': race_app.id}) }}">show</a>
                    <a href="{{ path('app_race_app_edit', {'id': race_app.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_race_app_new') }}">Create new</a>
{% endblock %}
