<form method="post" action="{{ path('app_inventory_app_delete', {'id': inventory_app.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inventory_app.id) }}">
    <button class="btn">Delete</button>
</form>
