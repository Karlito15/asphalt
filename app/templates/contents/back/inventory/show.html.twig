{% extends 'base.html.twig' %}

{% block title %}InventoryApp{% endblock %}

{% block body %}
    <h1>InventoryApp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inventory_app.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_inventory_app_index') }}">back to list</a>

    <a href="{{ path('app_inventory_app_edit', {'id': inventory_app.id}) }}">edit</a>

    {{ include('inventory_app/_delete_form.html.twig') }}
{% endblock %}
