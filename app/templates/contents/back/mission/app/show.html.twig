{% extends 'base.html.twig' %}

{% block title %}MissionApp{% endblock %}

{% block body %}
    <h1>MissionApp</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mission_app.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app.mission.index') }}">back to list</a>

    <a href="{{ path('app_mission_app_edit', {'id': mission_app.id}) }}">edit</a>

    {{ include('mission_app/_delete_form.html.twig') }}
{% endblock %}
