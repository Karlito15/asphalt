
                <div class="row row-cols-2">
                    <div class="w-25">
                        <a
                            href="{{ path(links.index) }}"
                            class="{{ classes.button.back }} w-100"
                        >
                            {{ 'btn.back'|trans }}
                        </a>
                    </div>
                    <div class="w-75">
                        <button type="submit" class="{{ classes.button.success }} w-100">
                            {{ 'btn.create'|trans }}
                        </button>
                    </div>
                </div>
