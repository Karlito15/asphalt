{% extends 'base.html.twig' %}

{% block title %}GarageEvo{% endblock %}

{% block body %}
    <h1>GarageEvo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ garage_evo.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_garage_evo_index') }}">back to list</a>

    <a href="{{ path('app_garage_evo_edit', {'id': garage_evo.id}) }}">edit</a>

    {{ include('garage_evo/_delete_form.html.twig') }}
{% endblock %}
