{% extends 'base.html.twig' %}

{% block title %}GarageBlueprint{% endblock %}

{% block body %}
    <h1>GarageBlueprint</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ garage_blueprint.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_garage_blueprint_index') }}">back to list</a>

    <a href="{{ path('app_garage_blueprint_edit', {'id': garage_blueprint.id}) }}">edit</a>

    {{ include('garage_blueprint/_delete_form.html.twig') }}
{% endblock %}
