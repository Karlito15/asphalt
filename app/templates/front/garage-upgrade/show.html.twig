{% extends 'base.html.twig' %}

{% block title %}GarageUpgrade{% endblock %}

{% block body %}
    <h1>GarageUpgrade</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ garage_upgrade.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_garage_upgrade_index') }}">back to list</a>

    <a href="{{ path('app_garage_upgrade_edit', {'id': garage_upgrade.id}) }}">edit</a>

    {{ include('garage_upgrade/_delete_form.html.twig') }}
{% endblock %}
