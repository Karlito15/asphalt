<form method="post" action="{{ path('app_garage_stat_actual_delete', {'id': garage_stat_actual.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ garage_stat_actual.id) }}">
    <button class="btn">Delete</button>
</form>
