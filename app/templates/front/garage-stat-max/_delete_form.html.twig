<form method="post" action="{{ path('app_garage_stat_max_delete', {'id': garage_stat_max.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ garage_stat_max.id) }}">
    <button class="btn">Delete</button>
</form>
