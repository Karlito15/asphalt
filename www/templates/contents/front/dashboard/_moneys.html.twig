
<section class="row">
    {{ component('FormInventory', {maxValue: 100, color: 'danger', datas: moneys.0, icon: null}) }}
    {{ component('FormInventory', {maxValue: 100, color: 'warning', datas: moneys.1, icon: null}) }}
    {{ component('FormInventory', {maxValue: 100, color: 'primary', datas: moneys.2, icon: null}) }}
    {{ component('FormInventory', {maxValue: 100, color: 'danger', datas: moneys.3, icon: null}) }}
    {{ component('FormInventory', {maxValue: 100, color: 'warning', datas: moneys.4, icon: null}) }}
    {{ component('FormInventory', {maxValue: 100, color: 'primary', datas: moneys.5, icon: null}) }}
    {{ component('FormInventory', {maxValue: 100, color: 'danger', datas: moneys.6, icon: null}) }}
</section>
