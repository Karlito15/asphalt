<form method="post" action="{{ path('app_race_region_delete', {'id': race_region.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ race_region.id) }}">
    <button class="btn">Delete</button>
</form>
