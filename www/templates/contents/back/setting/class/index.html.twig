{% extends '@App/bases/back/full.html.twig' %}

{% block table %}
<tbody>
{% for item in entities %}
    <tr>
        <td>{{ item.id }}</td>
        <td>{{ item.label }}</td>
        <td>{{ item.value }}</td>
        <td>{{ item.classOrder }}</td>
        <td>{{ item.carsNumber }}</td>
        <td>{{ item.median }}</td>
        <td>{{ item.slug }}</td>
        <td>{{ item.createdAt ? item.createdAt|date('Y-m-d H:i:s') : '' }}</td>
        <td>{{ item.updatedAt ? item.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
        <td>{{ item.deletedAt ? item.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
        <td><a href="{{ path('app.setting.class.update', {'id': item.id}) }}">edit</a></td>
    </tr>
{% else %}
    <tr>
        <td colspan="11">no records found</td>
    </tr>
{% endfor %}
</tbody>
<thead>
    {{ _self.tr() }}
</thead>
<tfoot>
    {{ _self.tr() }}
</tfoot>
{% endblock %}

{% macro tr() %}
<tr>
    <th>Id</th>
    <th>Label</th>
    <th>Value</th>
    <th>ClassOrder</th>
    <th>CarsNumber</th>
    <th>Median</th>
    <th>Slug</th>
    <th>CreatedAt</th>
    <th>UpdatedAt</th>
    <th>DeletedAt</th>
    <th>actions</th>
</tr>
{% endmacro %}
