<section class="card bg-primary py-0 mb-4 col-16">
    <div class="card-header"><h3 class="text-center pt-1">Stats</h3></div>
    <div class="card-body">
        <div class="card-group">
            {{ component('CardGarageStat', {
                form: true,
                max: form.statMax.0.speed,
                min: form.statMin.0.speed,
                background: 'bg-dark',
            }) }}
            {{ component('CardGarageStat', {
                form: true,
                max: form.statMax.0.acceleration,
                min: form.statMin.0.acceleration,
                background: 'bg-dark',
            }) }}
            {{ component('CardGarageStat', {
                form: true,
                max: form.statMax.0.handly,
                min: form.statMin.0.handly,
                background: 'bg-dark',
            }) }}
            {{ component('CardGarageStat', {
                form: true,
                max: form.statMax.0.nitro,
                min: form.statMin.0.nitro,
                background: 'bg-dark',
            }) }}
            {{ component('CardGarageStat', {
                form: false,
                max: garage.statMax.0.average,
                min: garage.statMin.0.average,
                background: 'bg-dark',
            }) }}
        </div>
    </div>
</section>
