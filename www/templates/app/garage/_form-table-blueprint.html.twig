<section class="card bg-primary py-0">
    <div class="card-header"><h3 class="text-center pt-1">Blueprints</h3></div>
    <div class="card-body">
        <div class="card-group">
            {{ component('CardGarageBlueprintForm', {
                maxValue: garage.settingBlueprint.star1,
                form: form.blueprint.0.star1,
            }) }}
            {{ component('CardGarageBlueprintForm', {
                maxValue: garage.settingBlueprint.star2,
                form: form.blueprint.0.star2,
            }) }}
            {{ component('CardGarageBlueprintForm', {
                maxValue: garage.settingBlueprint.star3,
                form: form.blueprint.0.star3,
            }) }}
            {{ component('CardGarageBlueprintForm', {
                maxValue: garage.settingBlueprint.star4,
                form: form.blueprint.0.star4,
            }) }}
            {{ component('CardGarageBlueprintForm', {
                maxValue: garage.settingBlueprint.star5,
                form: form.blueprint.0.star5,
            }) }}
            {{ component('CardGarageBlueprintForm', {
                maxValue: garage.settingBlueprint.star6,
                form: form.blueprint.0.star6,
            }) }}
            {{ component('CardGarageBlueprint', {
                maxValue: garage.settingBlueprint.total,
                total: garage.blueprint.0.total,
            }) }}
        </div>
    </div>
</section>
