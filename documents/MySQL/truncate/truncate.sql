SET FOREIGN_KEY_CHECKS=0;
TRUNCATE `app_garage`;
TRUNCATE `app_inventory`;
TRUNCATE `app_mission`;
TRUNCATE `app_race`;
TRUNCATE `garage_blueprint`;
TRUNCATE `garage_boolean`;
TRUNCATE `garage_rank`;
TRUNCATE `garage_stat_max`;
TRUNCATE `garage_stat_min`;
TRUNCATE `garage_upgrade`;
TRUNCATE `mission_task`;
TRUNCATE `mission_type`;
TRUNCATE `race_mode`;
TRUNCATE `race_region`;
TRUNCATE `race_season`;
TRUNCATE `race_time`;
TRUNCATE `race_track`;
TRUNCATE `setting_blueprint`;
TRUNCATE `setting_brand`;
TRUNCATE `setting_class`;
TRUNCATE `setting_level`;
TRUNCATE `setting_unit_price`;
TRUNCATE `ext_log_entries`;
SET FOREIGN_KEY_CHECKS=1;
